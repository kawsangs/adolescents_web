.container-fluid.px-4
  = content_for :header_title_right_col do
    = render "shared/buttons/back_button", url: categories_path
    = render "shared/buttons/add_new_button", url: new_category_importer_path, name: t('sidebar.categories')

  = render 'shared/header_title', title: t('shared.importing_history', name: t('sidebar.categories'))

  = render "shared/batches/batch_list"

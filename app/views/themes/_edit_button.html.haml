- if theme.default? && (current_user.admin? ||current_user.primary_admin?)
  = link_to edit_theme_path(theme), class: "btn btn-circle btn-sm btn-outline-warning me-1", data: { 'bs-toggle': 'tooltip', 'bs-placement': 'top'}, title: 'Built-in them: can be edited only theme background image' do
    %i.fas.fa-pen
- elsif policy(theme).update?
  = link_to edit_theme_path(theme), class: "btn btn-circle btn-sm btn-outline-warning me-1", data: { 'bs-toggle': 'tooltip', 'bs-placement': 'top'}, title: t('shared.edit') do
    %i.fas.fa-pen
- else
  %span{ data: { 'bs-toggle': 'tooltip', 'bs-placement': 'top' }, title: 'Cannot edit as it is already published!' }
    %button.btn.btn-circle.btn-sm.btn-outline-warning.me-1{ type: 'button', disabled: true }
      %i.fas.fa-pen

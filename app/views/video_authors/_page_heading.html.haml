= content_for :header_title_right_col do
  / Download button
  = render "shared/buttons/download_button", label: t('shared.download_json'), url: video_authors_path(filter_params.merge(format: :json))

= render 'shared/header_title', title: t('video.video_author')

/ Filter
.d-flex.my-2.align-items-center
  .flex-grow-1
    = render "filters"

  %div
    = render "videos/switch_view_buttons"
